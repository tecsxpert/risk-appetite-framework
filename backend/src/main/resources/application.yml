spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/risk_appetite_db
    username: postgres
    password: Prajwal@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080

ai:
  service:
    url: http://localhost:5000

jwt:
  secret: mysecretkey123456789